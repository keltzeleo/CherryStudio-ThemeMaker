# dependencies
node_modules/

# build output
dist/

# logs
*.log
npm-debug.log*

# macOS
.DS_Store

# OpenDesign metadata
*.artifact.json

# editor
.vscode/
.idea/

# unrelated stray project dropped inside the workspace (own git repo) — pollutes
# recursive `node --test` and `eslint .` (imports an uninstalled eslint-config-next)
agent-browser/
